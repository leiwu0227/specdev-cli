# Inventory: Tests

Area-4 inventory of `tests/` and `scripts/verify-*`. Read-only.

Repo root: `/mnt/h/oceanwave/lib/specdev-cli`.

The maintained test suite is intentionally compact per `.specdev/knowledge/architecture/reduced-test-suite.md` ("reduced-test-suite"). It targets user-blocking workflow surfaces — `init`/`update`, assignment + `.current` handling, workflow contract drift, knowledge/memory/distill basics, workflow-agent smoke, approve/checkpoint gates, and reviewloop pass/fail/autocontinue. Narrow implementation-detail tests should not exist in the maintained suite.

`npm test` (from `package.json`) chains, in order:
`test:init → test:assignment → test:workflow-contract-drift → test:update → test:knowledge → test:workflow-agent → test:approve-phase → test:checkpoints → test:reviewloop-command → test:cleanup`.

## Files

| Path | Purpose (one line) | Last meaningfully touched (commit subject) |
| --- | --- | --- |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/helpers.js` | Shared test helpers: `cleanupDir`, `runSpecdev` (spawn `bin/specdev.js` capturing stdout/stderr via tmp files), `createMockToolSkill`, `assertTest`. | fix: harden reviewloop parsing and stabilize test output capture |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-init.js` | Smoke for `specdev init`: essential file scaffolding, adapters (CLAUDE/AGENTS/.cursor), Claude+Codex command-skill installs, hook installation/idempotence/merging, `start` command surface. | refactor: thin workflow runtime guidance |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-assignment.js` | `specdev assignment` end-to-end: ID reservation, increment, `--type/--slug` folder + `.current`, `--discussion` promotion, `--json` shape, assignment utils (`parseAssignmentId`, `resolveAssignmentSelector`), and three schema-script cases (`scripts/verify-assignment-schema.js`). | Centralize workflow contract facts |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-workflow-contract-drift.js` | Asserts shipped templates/source stay in sync with `src/utils/workflow-contract.js` constants (assignment types, brainstorm sections, agent paths, runtime commands). | refactor: thin workflow runtime guidance |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-update.js` | `specdev update`: rewrites tampered command skills, backfills Codex skills, backfills `.specdev/agents`, refreshes hook script, preserves custom tools, restores `core/reviewloop`, removes legacy `tools/reviewloop`, migrates legacy `specdev-brainstorm`, runs sync. | Add agent templates and workflow contract |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-knowledge.js` | Smoke for `knowledge index/search/list`, hyphenated-term search, `memory refresh`, `distill --assignment` and `distill done`, auto-build of sqlite index. | test: reduce maintained suite |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-workflow-agent.js` | Smoke for `agents inspect` and `research` command using a stub runner injected into the researcher agent spec; covers happy path + malformed-output retry. | test: reduce maintained suite |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-approve-phase.js` | Unit tests for `src/utils/approve-phase.js` (`brainstorm` and `implementation` branches), using `node:test`. | refactor: extract approve-phase helper from approve command |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-checkpoints.js` | `specdev checkpoint` (brainstorm/implementation/discussion) + `specdev next --json` coverage: per-type required sections, autocontinue choice interaction, manifest validation (parse error, type error, null hook, invalid phases, scalar, empty, custom command, missing version), hook outcome surfacing. | refactor: thin workflow runtime guidance |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-reviewloop-command.js` | `specdev reviewloop` end-to-end: arg handling, reviewer listing, preflight, stale-feedback guard, approved + autocontinue contract, needs-changes verdict, timeout/log capture, stdout salvage, implementation approval, multi-reviewer stop-and-resume, `check-review` reviewer selection. | refactor: thin workflow runtime guidance |
| `/mnt/h/oceanwave/lib/specdev-cli/scripts/verify-assignment-schema.js` | Verifies an assignment dir against `specdev.assignment-schema.json` (required dirs, phase detection, optional paths). | feat: add legacy assignment migration command and tighten v4 workflow checks |
| `/mnt/h/oceanwave/lib/specdev-cli/scripts/verify-gates.sh` | Bash gate checker: detects LOW/MEDIUM/HIGH complexity, checks `breakdown/plan.md`, `scaffold/_architecture.md`, `implementation/progress.json`, V4 `tasks/*/result.md` or legacy `implementation.md`, `review_request.json` schema. | feat: add legacy assignment migration command and tighten v4 workflow checks |

Untracked / empty / unreferenced directories under `tests/` (not in git):

| Path | Tracked? | gitignored? | State |
| --- | --- | --- | --- |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-agent-runner-output/` | No | No | Empty directory; not referenced by any test or script. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-agents-inspect-output/` | No | No | Empty directory; not referenced by any test or script. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-research-output/` | No | No | Empty directory; not referenced by any test or script. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/tmp-hook-debug/` | No | Yes (`tests/tmp-*/`) | Snapshot of `specdev init` output left over from manual hook debugging. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/tmp-hook-debug2/` | No | Yes (`tests/tmp-*/`) | Same as above. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/tmp-review-continue/` | No | Yes (`tests/tmp-*/`) | Snapshot of `specdev init` output left over from manual review-continue debugging. |

## Claims & instructions

| Path | Claim/instruction (verbatim or paraphrased) | Where else this is asserted (if known) |
| --- | --- | --- |
| `tests/test-init.js` | After `specdev init`, ~18 essential files exist under `.specdev/` (workflow.yaml, _main.md, _index.md, _guides/workflow.md, _templates/gate_checklist.md, workflow_feedback_note.md, an example assignment, project_notes/big_picture.md, knowledge/_index.md, project_scaffolding/_README.md, core skills brainstorming/implementing/reviewloop/test-driven-development, tools/README.md, assignments/.gitkeep). | `templates/.specdev/` source tree; partial mirror in `tests/tmp-*/` fixtures. |
| `tests/test-init.js` | `.specdev/.gitignore` ignores `cache/`. | `templates/.specdev/.gitignore`. |
| `tests/test-init.js` | `workflow.yaml` declares `workflow_contract_version: 1` and includes the `repo_knowledge_prompt` hook id. | `templates/.specdev/workflow.yaml`; consumed by `test-checkpoints.js` manifest tests. |
| `tests/test-init.js` | `_main.md` contains the strings "SpecDev" and the "Specdev:" subtask-announce rule. | `CLAUDE.md` project instructions; `templates/.specdev/_main.md`. |
| `tests/test-init.js` | `_templates/workflow_feedback_note.md` includes the status field "Status: open \| mitigated \| resolved" and a "Proposed Action" section. | `templates/.specdev/_templates/workflow_feedback_note.md`. |
| `tests/test-init.js` | `skills/core/knowledge-capture/SKILL.md` is phase-end guidance referencing `workflow_feedback_note.md` and a "Prefer prune-and-replace" rule. | `test-workflow-contract-drift.js` asserts the same phrases in `templates/.specdev/skills/core/knowledge-capture/SKILL.md`. |
| `tests/test-init.js` | `knowledge/_index.md` distinguishes project-specific process knowledge from SpecDev workflow feedback. | `templates/.specdev/knowledge/_index.md`. |
| `tests/test-init.js` | Default init creates `CLAUDE.md`, `AGENTS.md`, and `.cursor/rules` all pointing to `.specdev/_main.md` and including the `templates/.specdev` source-of-truth rule and the "Specdev:" prefix instruction. | adapter generation in `src/commands/init.js`. |
| `tests/test-init.js` | Both `.claude/skills/` and `.codex/skills/` receive command skills: `specdev-start`, `specdev-assignment`, `specdev-rewind`, `specdev-continue`, `specdev-review`, `specdev-layout-migration`; `specdev-brainstorm` is explicitly removed (redundant with assignment). | `src/commands/init.js`; `test-update.js` migrates legacy `specdev-brainstorm`. |
| `tests/test-init.js` | `specdev-assignment` skill includes assignment-type list from `ASSIGNMENT_TYPES`, the `specdev next --json` runtime contract reference, and a "Specdev:" prefix instruction. Other generated skills also reference `specdev next --json` and contract-derived phase lists. | `src/utils/workflow-contract.js` (`ASSIGNMENT_TYPES`, `commandPhases`); duplicated drift check in `test-workflow-contract-drift.js`. |
| `tests/test-init.js` | `specdev-reviewloop` skill documents `--autocontinue` behavior, including "Do not stop after an approved autocontinue review" and "Carry the same reviewer forward". | `templates`/skill source. |
| `tests/test-init.js` | `specdev-layout-migration` skill references the migration guide and requires a `layout-plan.md`. | `.specdev/_guides/migration_guide.md`. |
| `tests/test-init.js` | `.claude/hooks/specdev-session-start.sh` exists, starts with `#!/usr/bin/env bash`, and is registered via `.claude/settings.json` `hooks.SessionStart`. Re-init with `--force` is idempotent (no duplicate hook entry); merging preserves existing keys (e.g., `permissions.allow`); invalid `settings.json` is left untouched. | `src/commands/init.js` hook installer. |
| `tests/test-init.js` | `--platform=claude` is preserved as backward-compatible shorthand but still creates all three adapters. | `src/commands/init.js`. |
| `tests/test-init.js` | `init --force` does NOT overwrite user-modified `CLAUDE.md`, `AGENTS.md`, or `.cursor/rules`. | `src/commands/init.js`. |
| `tests/test-init.js` | `init --dry-run --json` outputs `{ command: 'init', dry_run: true, to: <path ending in .specdev> }`. | `src/commands/init.js` JSON path. |
| `tests/test-init.js` | `specdev start` exits non-zero without `.specdev`, exits 0 with either template or filled `big_picture.md`. | `src/commands/start.js`. |
| `tests/test-assignment.js` | `specdev assignment <desc>` reserves an ID without creating the folder; the next call increments to `00002`. | `src/commands/assignment.js`. |
| `tests/test-assignment.js` | `assignment` without filled `big_picture.md` exits non-zero. | `src/commands/assignment.js` validation. |
| `tests/test-assignment.js` | Bare numeric label in non-interactive mode exits non-zero. | assignment resolver semantics. |
| `tests/test-assignment.js` | Bare `assignment` with no description exits non-zero and prints "No description" usage hint. | `src/commands/assignment.js`. |
| `tests/test-assignment.js` | `assignment --json` returns `{ id, description, status: 'ok', version: 1, assignments_dir }`. | `src/commands/assignment.js`. |
| `tests/test-assignment.js` | `scripts/verify-assignment-schema.js` accepts a valid review-phase assignment (prints "Schema checks passed"), accepts a partial brainstorm phase (prints `brainstorm artifacts satisfy "any" rule`), and rejects a missing `context/` dir (prints `context/ missing`). | `specdev.assignment-schema.json`; `scripts/verify-assignment-schema.js`. |
| `tests/test-assignment.js` | Utility `parseAssignmentId` parses standard, compound, and non-standard names; `assignmentName` extracts dir name on unix and windows paths; `resolveAssignmentSelector` handles explicit name, numeric shorthand, and ambiguous numeric. | `src/utils/assignment.js`. |
| `tests/test-assignment.js` | `assignment --type=<t> --slug=<s>` creates `00001_<type>_<slug>/{brainstorm,context}` and writes `.specdev/.current`. Unsupported `--type` errors with "Unknown assignment type" and creates no folder. Without `--type/--slug`, only an ID is reserved (no folder, no `.current`). | `src/commands/assignment.js`; `ASSIGNMENT_TYPES` in workflow contract. |
| `tests/test-assignment.js` | `assignment --discussion=Dxxxxx` copies `brainstorm/proposal.md` and `brainstorm/design.md` from the discussion to the new assignment folder, sets `.current`. Invalid discussion ID exits non-zero. | `src/commands/assignment.js` promotion path. |
| `tests/test-workflow-contract-drift.js` | Generated workflow guide, manifest, _main.md, brainstorm template, knowledge-capture skill, init command, command sources, and core skills all reference contract-derived values: assignment types joined by ` \| `, brainstorm section names, `specdev next --json`, artifact paths, agent paths/templates, and `workflow_contract_version: 1`. | `src/utils/workflow-contract.js`; mirrored in `tests/test-init.js`. |
| `tests/test-update.js` | `update` rewrites tampered Claude+Codex `specdev-assignment` skill and installs `specdev-layout-migration` skills. | `src/commands/update.js`. |
| `tests/test-update.js` | `update` backfills `.codex/skills/specdev-assignment/SKILL.md` when missing. | same. |
| `tests/test-update.js` | `update` backfills `.specdev/agents/researcher/agent.md` when missing; human output mentions agents; `update --dry-run --json` lists agents in `would_update`. | same. |
| `tests/test-update.js` | `update` rewrites tampered `.claude/hooks/specdev-session-start.sh`; restored script contains "SessionStart hook for specdev". | same. |
| `tests/test-update.js` | `update` preserves user-installed `.specdev/skills/tools/<custom>/SKILL.md`. | same. |
| `tests/test-update.js` | `update` restores tampered `.specdev/skills/core/reviewloop/SKILL.md`. | same. |
| `tests/test-update.js` | `update` removes the deprecated `.specdev/skills/tools/reviewloop` directory. | same. |
| `tests/test-update.js` | `update` migrates legacy slash skill layout: installs `specdev-assignment` and removes deprecated `specdev-brainstorm` for both Claude and Codex. | same. |
| `tests/test-update.js` | `update` runs sync that regenerates a removed tool-skill wrapper under `.claude/skills/<name>/SKILL.md`. | `src/commands/skills.js` sync path. |
| `tests/test-knowledge.js` | `knowledge index --json` outputs `{ command: 'knowledge index', status: 'ok', database_path: '.specdev/cache/knowledge.sqlite', document_count >= 6 }` and writes the sqlite file. | `src/commands/knowledge.js`. |
| `tests/test-knowledge.js` | `knowledge search "bounded memory"` prints `Knowledge Search: bounded memory`, the matching path, and a highlighted `Generated working [memory]` snippet. | same. |
| `tests/test-knowledge.js` | Hyphenated queries (`stream-json`, `agent-runner`, `workflow agent-runner`) return matches. | same. |
| `tests/test-knowledge.js` | `knowledge search --json` outputs `{ command: 'knowledge search', query, results: [{ path, ... }] }`. | same. |
| `tests/test-knowledge.js` | `knowledge list --json` outputs `{ command, files: [{ path, branch, title }], branches: { architecture: >= 2 } }` and includes nested files. Human list prints branch and nested path. | same. |
| `tests/test-knowledge.js` | Knowledge search auto-builds the index on first run. | same. |
| `tests/test-knowledge.js` | Unknown knowledge subcommand exits 1 with "Unknown knowledge subcommand". | same. |
| `tests/test-knowledge.js` | `memory refresh --json` returns `{ command: 'memory refresh', status: 'ok' }` and writes `.specdev/project_notes/working_memory.md`. | `src/commands/memory.js`. |
| `tests/test-knowledge.js` | `distill --assignment=<name> --json` returns `{ status: 'ok', capture: { project_notes_diff, workflow_diff } }` populated from `capture/*.md` files. | `src/commands/distill.js`. |
| `tests/test-knowledge.js` | `distill done <name> --json` returns `{ status: 'ok', marked: <name>, memory_hint: 'Run specdev memory refresh' }`. | same. |
| `tests/test-workflow-agent.js` | `agents inspect --json <path>` validates the researcher agent and returns `{ command: 'agents inspect', name: 'researcher', schema_path }` (status 0). | `src/commands/agents.js`. |
| `tests/test-workflow-agent.js` | Invalid agent spec (missing schema) yields `{ status: 'error' }` and a non-zero exit. | same. |
| `tests/test-workflow-agent.js` | `specdev research <topic> --platform=codex` writes `context/research-<topic>.md`; runner is invoked with `cwd == projectDir` and receives the rendered prompt on stdin. | `src/commands/research.js`. |
| `tests/test-workflow-agent.js` | When stub runner returns malformed output once, research retries; second invocation prompt contains "Previous response was rejected". | same. |
| `tests/test-approve-phase.js` | `approvePhase(assignment, 'brainstorm')` fails when `proposal.md`/`design.md` is missing or too short; on success, sets `status.brainstorm_approved = true` in `status.json` and preserves existing fields. | `src/utils/approve-phase.js`. |
| `tests/test-approve-phase.js` | `approvePhase(assignment, 'implementation')` fails when `progress.json` is missing/invalid/empty or has not-completed tasks; on success, sets `status.implementation_approved = true` and preserves existing fields. | same. |
| `tests/test-checkpoints.js` | `checkpoint brainstorm` enforces type-specific required sections: feature/refactor (Overview, Goals, Non-Goals, Design, Success Criteria); bugfix (Overview, Root Cause, Fix Design, Success Criteria); familiarization (Overview only); unknown types fall back to feature requirements. | `src/commands/checkpoint.js`. |
| `tests/test-checkpoints.js` | Heading matching is strict — "Overviewing", "Goals and Scope", etc., do not satisfy required headings. | same. |
| `tests/test-checkpoints.js` | Missing `proposal.md` always fails the brainstorm checkpoint. | same. |
| `tests/test-checkpoints.js` | `checkpoint brainstorm` (and implementation) emits a structured choice interaction with id `reviewloop_autocontinue`, mentioning `--autocontinue` and "Automated review only", plus reviewer-selection second-tier rules forbidding free-form reviewer text. | same. |
| `tests/test-checkpoints.js` | `specdev next --json` returns `{ next_action: { id: 'brainstorm.checkpoint' }, workflow: { contract_version: 1 }, interaction: { type: 'choice' }, trace: [...] }`. | `src/commands/next.js`. |
| `tests/test-checkpoints.js` | `next` reflects `workflow.yaml` customizations (e.g., custom `run:` command line is reported in `next_action.command_line`). | same. |
| `tests/test-checkpoints.js` | `next --json` returns `state: workflow_manifest_invalid` with appropriate blockers when manifest is missing version, has parse errors, has non-array hooks, has null hook entries, has non-object phases, is a scalar, or is empty. | `src/utils/workflow-manifest.js`. |
| `tests/test-checkpoints.js` | After implementation approval, `next` reports `state: completed` and surfaces advisory `repo_knowledge_prompt` hook with `outcome: skipped`. | `src/commands/next.js`. |
| `tests/test-checkpoints.js` | `checkpoint discussion --discussion=Dxxxxx` prints `specdev reviewloop discussion --discussion=Dxxxxx --reviewer=<name>` but does NOT print autocontinue or "continue if approved". | `src/commands/checkpoint.js`. |
| `tests/test-checkpoints.js` | Tool skills are advisory in implementation checkpoint — checkpoint passes even when tasks declare skills like `mock-tool`; warnings include `TOOL_SKILL_SKIPPED` when a task has a `**Skipped:**` waiver. | same. |
| `tests/test-reviewloop-command.js` | Missing phase exits 1 with "Missing required phase argument"; unknown phase exits 1 with "Unknown reviewloop phase". | `src/commands/reviewloop.js`. |
| `tests/test-reviewloop-command.js` | Without `--reviewer`, listing prints "Available reviewers:" with configured reviewers plus an `--autocontinue` example; `--json` returns `{ status: 'ok', reviewers: [...] }`. | same. |
| `tests/test-reviewloop-command.js` | `--preflight --json` rejects reviewers with `missing_command` issue; without preflight, normal run exits 1 with "Reviewer preflight failed" and does not write feedback. | same. |
| `tests/test-reviewloop-command.js` | Re-running with stale needs-changes feedback exits 1 with "Previous review findings have not been addressed". Writing a `*-changelog*.md` bypasses the guard. | same. |
| `tests/test-reviewloop-command.js` | Approved verdict with `--autocontinue` prints `"mode": "autocontinue"` and `"next_action_command": "specdev next --json"`, never hardcodes `"next_phase": "breakdown"`, and writes `status.brainstorm_approved = true`. | same. |
| `tests/test-reviewloop-command.js` | Needs-changes verdict exits 0 (within max rounds) and prints "Run specdev check-review to address findings". | same. |
| `tests/test-reviewloop-command.js` | Reviewer timeout uses `SPECDEV_REVIEWER_TIMEOUT` env override; on timeout, exits 1 with "Reviewer timed out after 1s" and writes a round log with "Timed out:  true". | same. |
| `tests/test-reviewloop-command.js` | Reviewer log captures stdout + stderr + verdict for each round under `review/<phase>-reviewer-<name>-round-<n>.log`; path is printed via "Reviewer log:". | same. |
| `tests/test-reviewloop-command.js` | When reviewer writes feedback to stdout instead of file, output is salvaged into `<phase>-feedback.md` with a "salvaged from stdout" marker. | same. |
| `tests/test-reviewloop-command.js` | Implementation reviewloop approval writes `status.implementation_approved = true`. | same. |
| `tests/test-reviewloop-command.js` | Multi-reviewer (`--reviewer=a,b`) does not approve phase until both pass; an already-approved reviewer is skipped with "already approved, skipping". | same. |
| `tests/test-reviewloop-command.js` | `check-review --reviewer=<name>` reads `<phase>-feedback-<name>.md`; without `--reviewer`, it auto-detects the reviewer-specific feedback file. | `src/commands/check-review.js`. |
| `scripts/verify-assignment-schema.js` | Validates an assignment dir against `specdev.assignment-schema.json` (required dirs, phase artifact mode all/any, optional paths). Prints "Schema checks passed" on success. | invoked from `tests/test-assignment.js`. |
| `scripts/verify-gates.sh` | Asserts an assignment has `breakdown/plan.md`, complexity class (LOW/MEDIUM/HIGH), `implementation/progress.json`, V4 `tasks/*/result.md` OR legacy `implementation.md` with TDD table rows, `review_request.json` with strict v1 fields, brainstorm artifacts, optional `review_report.md`, and `context/`+`tasks/` dirs. | Referenced as "Pre-flight (verify-gates.sh)" in `templates/.specdev/_templates/review_report_template.md`. NOT invoked by `package.json`, hooks, or any current test. |

## References

| Path | References these (paths/commands/skills it points to) | Referenced by (paths that mention it) |
| --- | --- | --- |
| `tests/helpers.js` | `bin/specdev.js`; `/tmp/specdev-test-*.std{out,err}`; `.specdev/skills/tools/<name>/SKILL.md`. | `tests/test-assignment.js`, `tests/test-knowledge.js`, `tests/test-workflow-agent.js` (import `cleanupDir`, `runSpecdev`, `assertTest`); `tests/test-checkpoints.js`, `tests/test-update.js` (import `createMockToolSkill` only). |
| `tests/test-init.js` | `bin/specdev.js`; `src/commands/init.js`; `src/utils/workflow-contract.js` (`ASSIGNMENT_TYPES`, `commandPhases`); templates under `templates/.specdev/`. | `package.json` `test:init`. |
| `tests/test-assignment.js` | `tests/helpers.js`; `src/commands/assignment.js`; `src/utils/assignment.js`; `scripts/verify-assignment-schema.js` (sole consumer in repo); `bin/specdev.js`. | `package.json` `test:assignment`. |
| `tests/test-workflow-contract-drift.js` | `src/utils/workflow-contract.js`; `templates/.specdev/_guides/workflow.md`, `templates/.specdev/workflow.yaml`, `templates/.specdev/_main.md`, `templates/.specdev/_templates/brainstorm-design.md`, `templates/.specdev/skills/core/{knowledge-capture,brainstorming,breakdown,implementing,reviewloop}/SKILL.md`, `templates/.specdev/agents/researcher/agent.md`, `templates/.specdev/agents/researcher/output-schema.json`, `templates/.specdev/_templates/agent-spec.schema.json`, `templates/.specdev/_index.md`; `src/commands/{init,approve,implement,check-review}.js`. | `package.json` `test:workflow-contract-drift`. |
| `tests/test-update.js` | `tests/helpers.js`; `src/commands/update.js`; `bin/specdev.js`. | `package.json` `test:update`. |
| `tests/test-knowledge.js` | `tests/helpers.js`; `src/commands/{knowledge,memory,distill}.js`; `bin/specdev.js`. | `package.json` `test:knowledge`. |
| `tests/test-workflow-agent.js` | `tests/helpers.js`; `src/commands/{agents,research}.js`; `templates/.specdev/agents/researcher/agent.md` (via `init` copy); `bin/specdev.js`. | `package.json` `test:workflow-agent`. |
| `tests/test-approve-phase.js` | `src/utils/approve-phase.js`; `node:test`. | `package.json` `test:approve-phase`. |
| `tests/test-checkpoints.js` | `tests/helpers.js` (`createMockToolSkill` only); `src/commands/{checkpoint,next}.js`; `src/utils/workflow-manifest.js`; `bin/specdev.js`. | `package.json` `test:checkpoints`. |
| `tests/test-reviewloop-command.js` | `src/commands/{reviewloop,check-review}.js`; `bin/specdev.js`. | `package.json` `test:reviewloop-command`. |
| `scripts/verify-assignment-schema.js` | `specdev.assignment-schema.json`. | `tests/test-assignment.js`; `docs/assignment-schema.md`. NOT referenced from `package.json` scripts, `hooks/`, or `templates/`. |
| `scripts/verify-gates.sh` | `breakdown/plan.md`, `scaffold/_architecture.md`, `implementation/progress.json`, `tasks/*/result.md`, `implementation.md`, `review_request.json`, `review_report.md`, `context/`, `tasks/`. | `.specdev/_templates/review_report_template.md` + `templates/.specdev/_templates/review_report_template.md` ("Pre-flight (verify-gates.sh)"); `docs/plans/2026-02-11-*`, `docs/plans/2026-02-12-*`, `docs/plans/2026-02-14-*` (historical plan docs). NOT referenced from `package.json`, `hooks/`, or any current test. |
| `tests/test-agent-runner-output/`, `tests/test-agents-inspect-output/`, `tests/test-research-output/` | (empty; nothing referenced) | NOT referenced from any `*.js`, `*.sh`, `*.json`, or `*.md` file in the repo. `package.json` `test:cleanup` removes `tests/test-init-output`, `./test-assignment-output`, `tests/test-assignment-schema-output`, `tests/test-update-output`, `tests/test-knowledge-output`, `tests/test-workflow-agent-output`, `tests/test-approve-phase-output`, `tests/test-reviewloop-command-output`, `tests/test-checkpoints-output` — but NOT these three empty dirs. |
| `tests/tmp-hook-debug/`, `tests/tmp-hook-debug2/`, `tests/tmp-review-continue/` | Snapshot copies of `templates/.specdev/` + adapter files. | NOT referenced from any `*.js`, `*.sh`, `*.json`, or `*.md` file. Covered by `.gitignore` (`tests/tmp-*/`). |

## Within-area findings

| Path | Type | Severity | Note | Suggested action |
| --- | --- | --- | --- | --- |
| `/mnt/h/oceanwave/lib/specdev-cli/scripts/verify-gates.sh` | legacy | medium | Asserts pre-V4 artifacts (`scaffold/_architecture.md`, legacy `implementation.md` with TDD tables, `review_request.json` with `gate_3`/`gate_4`) that are no longer produced by the current workflow (no template scaffolds them, no command creates `review_request.json`). Only referenced from the `review_report_template.md` "Pre-flight" pasteboard and three historical plan docs under `docs/plans/`. Not wired into `package.json`, hooks, or any maintained test. Drifts from current schema and the reduced-suite scope. | Decide: delete `scripts/verify-gates.sh` and the "Pre-flight (verify-gates.sh)" block in both `_templates/review_report_template.md` copies, OR rewrite it to match `specdev.assignment-schema.json` and wire it into `npm test`. |
| `/mnt/h/oceanwave/lib/specdev-cli/.specdev/_templates/review_report_template.md` and `/mnt/h/oceanwave/lib/specdev-cli/templates/.specdev/_templates/review_report_template.md` | inconsistency | medium | Both templates instruct reviewers to paste output of `verify-gates.sh`, but the script is unmaintained and references retired artifacts. | Remove the verify-gates pasteboard section, or replace it with the maintained `scripts/verify-assignment-schema.js` (which the test suite actually exercises). |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-agent-runner-output/` | legacy | low | Empty, untracked directory left by an older test layout (likely from the agent-runner work before the suite was reduced). Not in `.gitignore`, not in `test:cleanup`, not referenced anywhere. Adds noise to `tests/`. | Delete the directory. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-agents-inspect-output/` | legacy | low | Same as above — empty leftover from prior `agents inspect` tests. | Delete the directory. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-research-output/` | legacy | low | Same as above — empty leftover from prior `research` test fixture. | Delete the directory. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/tmp-hook-debug/` | legacy | low | Gitignored stale debug snapshot of `specdev init` output (kept locally only). Mostly harmless because of `.gitignore`, but it clutters the working tree and could be mistaken for fixture data. | Delete locally; consider documenting the `tmp-*/` convention in `tests/README` if one is added. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/tmp-hook-debug2/` | legacy | low | Same as above. | Delete locally. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/tmp-review-continue/` | legacy | low | Same as above; older than the hook-debug snapshots (Mar 12 dated). | Delete locally. |
| `/mnt/h/oceanwave/lib/specdev-cli/package.json` (`scripts.test:cleanup`) | inconsistency | low | Cleanup script does not include `tests/test-agent-runner-output`, `tests/test-agents-inspect-output`, `tests/test-research-output`, even though their naming follows the same `tests/test-*-output` convention as the cleaned dirs. Also still lists `./test-assignment-output` at the repo root (not under `tests/`) — `tests/test-assignment.js` actually uses `TEST_DIR = './test-assignment-output'` (resolved from cwd), so this entry is correct but anomalous vs. the others. | If the three orphan dirs are removed, drop them entirely; otherwise, add them to `test:cleanup`. Consider relocating `test-assignment.js`'s `TEST_DIR` under `tests/` for consistency. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-init.js` | inconsistency | low | Uses a local `assert/runCmd/cleanup` trio (lines 14-30) instead of importing `runSpecdev`/`assertTest`/`cleanupDir` from `tests/helpers.js`. Five of the other ten test files use the shared helpers; `test-init.js` and `test-checkpoints.js` re-implement spawn capture inline. | Refactor `test-init.js` and `test-checkpoints.js` to use `tests/helpers.js` for spawn capture + assertions. Low priority — purely a hygiene win. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-checkpoints.js` | inconsistency | low | Same as above — duplicates `runSpecdev`-style spawn-with-fd capture inline rather than importing `runSpecdev` from `helpers.js`. Only imports `createMockToolSkill`. | Switch to `runSpecdev` and `assertTest`. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-init.js` | inconsistency | low | Embedded checkpoint-language assertions live in `test-checkpoints.js` (autocontinue/reviewer choice strings); `test-init.js` separately asserts those same strings appear inside the generated skill prose. This is intentional drift-detection coupling but it duplicates string fixtures between the two files. | Consider centralizing the autocontinue/choice phrases as exported constants in `src/utils/workflow-contract.js` so both tests cite a single source. Optional. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-checkpoints.js` | inconsistency | low | Mixes two responsibilities under one file: `checkpoint` gate checks AND `next --json` workflow-manifest validation (parse error, type error, scalar, empty, null hook, missing version, custom command). The latter is `next` behavior — slightly off-name. | Either rename the file (e.g., `test-checkpoints-and-next.js`) or split the manifest-validation block into a dedicated `test-next.js`. Cosmetic. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/test-approve-phase.js` | inconsistency | low | Only file using `node:test` (`describe`/`it`/`beforeEach`/`afterEach`); every other test file uses ad-hoc `assert()` runners + custom `pass/fail` counters. Mixed test styles in a maintained suite. Per the reduced-test-suite policy this also leans toward "narrow implementation-detail" — it exercises an internal helper (`src/utils/approve-phase.js`) rather than a command flow, though approve/checkpoint gates ARE explicitly in scope. | Either accept the mixed style (status quo seems intentional after the `extract approve-phase helper` refactor) or rewrite to the same plain runner style as the rest of the suite. If keeping `node:test`, document the convention. |
| `/mnt/h/oceanwave/lib/specdev-cli/tests/helpers.js` | inconsistency | low | All four exports are used somewhere in `tests/*.js`. Helper is fine, but two consumers (`test-checkpoints.js`, `test-update.js`) import ONLY `createMockToolSkill` and re-implement spawn capture and assertions inline. | See findings on those two files. No change to `helpers.js` itself required. |
| `/mnt/h/oceanwave/lib/specdev-cli/.gitignore` | inconsistency | low | Includes `tests/tmp-*/` but NOT `tests/test-*-output/`. Since `test:cleanup` removes most `test-*-output` dirs after each run, and these are clearly transient, they belong in `.gitignore` too — that would also prevent the kind of stale empty dirs flagged above (`test-agent-runner-output`, etc.) from accumulating in working trees. | Add `tests/test-*-output/` to `.gitignore`. |
