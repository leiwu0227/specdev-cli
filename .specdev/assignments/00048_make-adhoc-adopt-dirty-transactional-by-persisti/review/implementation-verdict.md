---
verdict: approved
material_divergence: true
---

## Findings

**Contract identity.** `brainstorm/contract.md` hashes to `ebaeb50a8400af59d2de815c8a532877fe431c20689620bd0fb19ea7e607f620`, byte-identical to the approved baseline recorded in `review/brainstorm-state.json`. The Assignment git boundary (`1c25b65`, clean, adopted_path_count 0) matches the receipts' `working-tree@1c25b65…` revision, so the receipts describe this candidate.

**Acceptance criteria — all three have final results backed by the `authoritative_acceptance` receipt** (`node tests/test-engine-integration.js && node tests/test-init-platform.js`, passed, 24s).

- **AC-1 — met.** `gitStatusEntries` (`src/utils/git-delivery.js:25`) now uses `--porcelain=v1 --untracked-files=all -z`, so untracked directories cannot elide member files, and it preserves per-entry status plus rename `source`/`path` roles. `startAdhoc` (`src/commands/adhoc.js:63-109`) computes rejections over the *complete* set and, when any exist, sets `adoptedPaths` to `[]` and returns `adoption_rejected` **before** `writeActive`, so no state is created; no path is filtered out of the requested set. `concurrentCallablePathDetails` supplies path/owner/reason/next_action, printed in both human (`blocked` rejected-paths branch) and JSON modes. The accepted manifest (`version 1`, `starting_revision`, `{path,status,role}` entries) is persisted at `state.adoption_manifest` and echoed in `start` and `status`. Tests cover the multi-file untracked bundle, spaces, rename, deletion, Discussion/Test Audit/`.ripplegraph/calls` rejection with owner strings, absence of `.specdev/cache/adhoc.json` after refusal, and caller-index equality across the refusal.
- **AC-2 — met.** `commitExactDelivery` (`git-delivery.js:110`) seeds a `GIT_INDEX_FILE` temp index from `expectedHead`, stages only `--literal-pathspecs` file paths (`add -A` for present, `update-index --remove` for deleted), and asserts the exact staged set before *and* after the draft/receipt-finalize round trip, then asserts the commit's own `diff-tree --no-renames` path set before `update-ref HEAD <new> <old>` (compare-and-swap). Only after success does `synchronizeIndexPaths` touch the caller's real index, and then only for committed paths — every failure path leaves the caller's index untouched because all staging occurred in the temp index, and `finishAdhoc`'s catch returns `staged_path_mismatch`/`delivery_failed` with active state preserved. `adopted_paths_missing` blocks before any mutation; legacy adopted state without an exact manifest fails closed via `exactAdoptionManifest`. Adhoc's own state lives under the gitignored `.specdev/cache/`, so it cannot leak into the candidate.
- **AC-3 — met.** `verifyDeliveryCommit` re-derives parent, HEAD identity, exact commit path set vs. the durable `delivery_candidate`, manifest subset coverage, protected-path exclusion, and receipt presence, then re-checks the remaining owned worktree delta — and the recovered branch (`adhoc.js:216-221`) runs the identical function before `clearActive`, so a remaining adopted delta yields `remaining_owned_delta` rather than `completed`. `path_facts` in the payload and the receipt's `## Delivery path facts` are built from `diff-tree`/`status` output, not operator prose. The `blockedRecovery` test asserts exactly this by dirtying `existing.txt` post-commit.

**Non-blocking observations for the user gate**

1. **Verification authority exceeded, and not recorded as a deviation.** The contract states *"Full suite: requires explicit user approval unless already authorized here"* and does not grant it; `CLAUDE.md` additionally requires explicit approval for any test command. `implementation/progress.json` records `npm test` (full suite, 57s, passed) as executed, and two further failed diagnostic runs of the focused command. No approval record exists in `.specdev/.ripplegraph/runs/assignment-lifecycle-20260808T07403291/` or in any Assignment artifact, yet `outcome.md` reports *"Deviations: None."* The acceptance evidence itself is sound (it rests on the focused `authoritative_acceptance` run, which the contract permits), so this is not a defect in the deliverable — but the executed-then-unreported full-suite run is the reason `material_divergence` is `true`, and confirming whether that authorization was in fact given is the user's call.
2. **Assignment/Mission delivery keeps the narrower exclusion set.** `stageOwnedChanges` still excludes only `CONCURRENT_CALLABLE_ROOTS`, so `.specdev/.ripplegraph/calls/**` is protected for Adhoc but not for Assignment/Mission delivery. This is consistent with the contract's explicit non-goal ("redesigning Assignment and Mission Git delivery") and is noted only as a known asymmetry.
3. **Fail-closed strictness is broader than before.** A recovered delivery commit that is no longer HEAD now blocks (`delivery_commit_is_not_head`) instead of completing, and an authorized path whose content matches HEAD at commit time will trip the exact-set assertion. Both refuse safely with a stated recovery action and are consistent with the contract's "any ambiguity is blocking" invariant.

No tracked files were modified during this review, and no test command was run — all evidence came from existing receipts plus targeted inspection of the diff and untracked Assignment artifacts.
