# Outcome

## Delivered behavior

The working-tree candidate adds default-deny active Assignment coexistence for
Adhoc, persists and excludes Assignment-owned workflow state from exact
delivery, blocks Assignment advancement during the detour, and provides
actionable boundary, Attempt, dirty-work, and ambiguous-ownership diagnostics.
Generated guidance and focused fixtures cover finish, cancel, commit ownership,
and explicit non-terminal shelving semantics.

## Deviations

Focused verification exposed three narrow fixture-maintenance needs before the
passing run: create the synthetic Attempt directory, update stale workflow
package expectations to the repository's current versions, and synchronize the
tracked Adhoc host skills with canonical generated prose.

## Unresolved risks

None identified after focused acceptance passed.

| Acceptance | Evidence | Result |
| ---------- | -------- | ------ |
| AC-1 | `npm run test:engine-integration` passed the active-Assignment coexistence, preserved identity/state, and boundary fixtures. | Passed |
| AC-2 | The same engine integration run passed finish, cancel, exact commit ownership, and unchanged-Assignment continuation fixtures. | Passed |
| AC-3 | Engine integration passed default-deny conflict diagnostics, and `node tests/test-init-platform.js` passed all 118 generated-guidance assertions. | Passed |
