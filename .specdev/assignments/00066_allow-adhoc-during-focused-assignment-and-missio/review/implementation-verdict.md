---
verdict: approved
material_divergence: false
scope_divergence: none
procedure_divergence: none
evidence_integrity: complete
user_reapproval_required: false
---

## Findings

**Evidence integrity — complete.** All four artifact digests recomputed at review time match the candidate receipt exactly (contract `29c7d4c7…`, plan `022f6c23…`, progress `55d9e458…`, outcome `20756f94…`). Every verification receipt is anchored at `working-tree@5311a6f77ed32b7cded383727abcd557431867b1`, which is the current HEAD. The latest receipt for each of the four commands is `passed`; the three superseded failures are the same three fixture-only repairs disclosed in `outcome.md` "Deviations" and `progress.json`. File mtimes confirm no source, template, or test file was modified after the final passing `npm run test:engine-integration` run, so no post-verification drift exists. `changed_project_paths` (15) matches the working tree exactly; the only other dirty paths are `.specdev/` runtime state, which AGENTS.md classifies as installed runtime, not product source.

**Procedure — none.** Verification used four focused sub-suites (`test:engine-integration`, `test:engine-packaging`, `test-status-visibility.js`, `test-init-platform.js`), not `npm test`. That is within the contract's "focused tests for changed modules" authority, and `progress.json` records the runs as authorized. No full suite was run, and I ran none during review.

**Scope — none.** The delivered code matches the approved contract and the T-1/T-2/T-3 plan. `src/utils/adhoc-focused.js` implements the owner-neutral classifier with a closed `SAFE_POSITIONS` allowlist (assignment: brainstorm/approve-contract/design; mission: brainstorm/approve-mission), run/checkpoint agreement checks, `worker|reviewer|controller` Attempt-liveness conflicts, and fail-closed `ambiguousOwner`/`boundaryBlocked`/`revalidationBlocked` diagnostics carrying `next_action`. Mission positions after approval remain blocked via `base_revision || approved_contract_hash || approved_at`; the contract explicitly permits modelling safe positions as an allowlist, and the restriction is disclosed under "Unresolved risks". `dispatch.js` blocks the full advancing-command set both during the detour and while an obligation is pending, exempting only explicit terminal resolutions (`cancel`, `assignment shelf|close`, `mission abandon`); `discussion`, `test-audit`, `roadmap`, `context`, and `adhoc revalidate` remain reachable, preserving the required concurrent-callable compatibility. Finish and cancel both write the durable `adhoc_revalidation` record (revision pair + changed paths) before clearing active state, and `status-view.js` projects it as `blocked` with the recovery command. Legacy `assignment_coexistence` state (v2) is still read via `activeFocusedCoexistence`, so in-flight detours are not orphaned. Guidance in `templates/.specdev/_main.md`, `_guides/workflow.md`, `init.js` embedded help, `QUICKSTART.md`, and both generated SKILL.md files states the same rule set, and `test-init-platform.js` asserts that consistency.

**Acceptance.** AC-1, AC-2, and AC-3 each have a final `passed` result with named fixture coverage; added assertions exercise Assignment brainstorm/approve-contract detours, Mission brainstorm/approve-mission detours, guard blocking, exact finish/cancel path facts, `changed_paths` contents, blocked `status`/`next` projection, unchanged- and changed-contract revalidation, live-Attempt conflict, established-boundary conflict, and unsupported-position ambiguity. No acceptance item is missing or blocked. No external dependency was added or upgraded, so no registry/lockfile evidence is required. `package.json` `releaseDate` is already `2026-09-02`, satisfying the pre-commit repository instruction.

**Non-blocking observations (no action required for delivery):**
- `revalidateAdhoc` passes `publicOwner` (kind/id/name/run_id only) to `focusedOwnedPathDetails`, so `preserved_paths` and `child_paths` are absent during the revalidate path. Owner-root, run-root, `.specdev/processes/`, and `.specdev/.ripplegraph/runs/` prefixes still cover today's cases, and Mission children cannot exist at a permitted Mission position, so this is currently unreachable; it would only matter if child-bearing positions were ever added to the allowlist.
- `writeOwnerRecord` inlines a third private copy of the temp-file-plus-move JSON write already duplicated in `mission-reapproval.js` and `mission-successor-adoption.js`. It matches the existing repo pattern; a shared helper would be a reasonable future cleanup.
- A narrow `prettier --check` over the changed linted files passes. It also reports two pre-existing violations in `src/utils/mission-reapproval.js` and `src/utils/review-convergence.js`, which this candidate does not touch; CI runs `npm run lint`, so that is worth separate attention but is not attributable to this candidate.