# Task Spec: Knowledge Directory Templates

## Objective
Create the `knowledge/` directory structure in `templates/.specdev/`.

## Deliverables
- `knowledge/_index.md` — routing file explaining branches and growth model
- `knowledge/_workflow_feedback/.gitkeep` — global candidate collection point
- `knowledge/codestyle/.gitkeep` — local branch
- `knowledge/architecture/.gitkeep` — local branch
- `knowledge/domain/.gitkeep` — local branch
- `knowledge/workflow/.gitkeep` — local branch
