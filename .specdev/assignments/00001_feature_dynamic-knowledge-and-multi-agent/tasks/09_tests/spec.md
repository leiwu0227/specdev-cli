# Task Spec: Update Tests

## Objective
Update `tests/verify-output.js` to verify the new knowledge/ directory structure. Also fix existing bug where `main.md` and `router.md` were checked without underscore prefix.

## Blocked By
- Task 01 (knowledge templates)
- Task 02 (assignment example)
