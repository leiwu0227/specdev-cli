# Task Result: CLI Routing & Help

## Status: Complete

## Files Modified
- `bin/specdev.js` — added imports for ponder commands, added `case 'ponder'` with subcommand routing (workflow|project), error on unknown subcommand
- `src/commands/help.js` — added `ponder workflow` and `ponder project` to commands list and examples
