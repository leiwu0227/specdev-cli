# Task Spec: Assignment Example Update

## Objective
Add `context/` and `tasks/` subdirectories to the existing email-validator example assignment in templates.

## Deliverables
- `context/decisions.md` — example decisions log with 2 sample decisions
- `context/progress.md` — example progress tracking showing completed state
- `context/messages/.gitkeep` — empty messages directory
- `tasks/_index.md` — example task index with status and dependencies
- `tasks/01_validator/spec.md` — example task spec
- `tasks/01_validator/result.md` — example task result
