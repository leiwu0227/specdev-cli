# Implementation Changelog

## Round 1

- Addressed minor help text finding F1.2: `--assignment` now says it applies to `distill` and `migrate legacy-assignments`, not bare `migrate`.
- Left F1.1 as a future cleanup because the hardcoded init skill summary is a broader pre-existing pattern that also omits other command skills.
