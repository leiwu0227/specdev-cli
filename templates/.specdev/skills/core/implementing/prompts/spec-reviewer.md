# Spec Compliance Reviewer (Compatibility)

This prompt is retained for backward compatibility.
Use `code-reviewer.md` as the primary reviewer prompt; it now includes spec compliance and quality checks in one pass.
