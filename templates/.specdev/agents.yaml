implementation:
  mode: auto # auto | inline | spawned

worker:
  provider: codex
  model: gpt-5
  effort: high
  network: false
  timeout: 60m

reviewer:
  provider: claude
  model: opus
  effort: high
  network: false # Codex reviewers may opt in; unsupported provider policies fail before launch.
  timeout: 20m
