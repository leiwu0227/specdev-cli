// Stub — Task 2 will implement the full distill done command
export async function distillDoneCommand(positionalArgs = [], flags = {}) {
  console.error('distill done is not yet implemented')
  process.exitCode = 1
}
